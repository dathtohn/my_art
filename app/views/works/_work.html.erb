<div>
	<%= work.title %>

	<% if signed_in? %>
	  | <%= link_to "edit", edit_collection_work_path(@collection, work) %>
	  | <%= link_to "delete", work, method: :delete, data: { confirm: "You sure?" } %>
	<% end %>
	
	<br>
  <%= link_to image_tag(work.photo(:thumb), size: '200x200'), work.photo.url, class: 'fancybox', rel: 'collection' %>
</div>