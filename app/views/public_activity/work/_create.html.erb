created a new work  

<% if activity.trackable %>
  titled <%= link_to image_tag(activity.trackable.photo(:thumb), size: "30x30"), activity.trackable.collection %> <%= link_to activity.trackable.title, activity.trackable.collection %> in the <%= link_to image_tag(activity.trackable.collection.cover(:thumb), size: "30x30"), activity.trackable.collection.user %> <%= link_to activity.trackable.collection.title, activity.trackable.collection %> collection.
<% else %>
  which has since been removed.
<% end %>