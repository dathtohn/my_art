created a new work  

<% if activity.trackable %>
  titled <%= activity.trackable.title %> in the <%= link_to activity.trackable.collection.title, activity.trackable.collection %> collection.
<% else %>
  which has since been removed.
<% end %>